services:
  db:
    image: mysql:9.6.0
    restart: always
    ports:
      - '3307:3306'
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: events
    volumes:
      - mysql-data:/var/lib/mysql
volumes:
  mysql-data: